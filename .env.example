POSTGRES_DB=self_healing
POSTGRES_USER=self_healing
POSTGRES_PASSWORD=self_healing
CONTROL_DATABASE_URL=postgresql://self_healing:self_healing@postgres:5432/self_healing
TARGET_POSTGRES_DB=target_app
TARGET_POSTGRES_USER=target_app
TARGET_POSTGRES_PASSWORD=target_app
TARGET_DATABASE_URL=postgresql://target_app:target_app@target-db:5432/target_app
MONITOR_INTERVAL_SECONDS=10
TARGET_API_BASE_URL=http://target-api:8001
TARGET_REQUIRED_SECRET=runtime-foundation-secret

# Local development keeps auth off. Production deployments should enable auth
# and inject API_KEYS or API_KEYS_FILE from a secret manager.
AUTH_ENABLED=false
API_KEYS=replace-with-dev-only-key
LOG_LEVEL=INFO

# Optional Firecracker supervisor. Leave blank for local Docker-only runs.
FIRECRACKER_API_URL=
FIRECRACKER_TIMEOUT_SECONDS=10

# Optional Claude-backed reasoning. Disabled by default so local demos remain
# deterministic and offline-friendly.
LLM_REASONING_ENABLED=false
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest
ANTHROPIC_API_URL=https://api.anthropic.com/v1/messages
ANTHROPIC_TIMEOUT_SECONDS=30

# Optional Docker/Kubernetes-style file secret support:
# API_KEYS_FILE=/run/secrets/control_api_keys
# DATABASE_URL_FILE=/run/secrets/control_database_url
# ANTHROPIC_API_KEY_FILE=/run/secrets/anthropic_api_key
